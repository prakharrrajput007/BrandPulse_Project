export function isWebContainer() {
    return process.env.SHELL === '/bin/jsh' && !!process.versions.webcontainer;
}
export function getPlatformFromEnvironment() {
    if (isWebContainer()) {
        return 'bolt';
    }
    return process.platform;
}
