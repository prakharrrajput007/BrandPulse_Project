import { type OperatorFunction } from 'rxjs';
/**
 * Internal helper function, same as rxjs distinct, but adding an LRU flavor to keep memory bounded.
 */
export declare function lruDistinct<T, K>(keySelector?: (value: T) => K, maxKeys?: number): OperatorFunction<T, T>;
//# sourceMappingURL=lru-distinct.d.ts.map