import { Header } from '@devvit/shared-types/Header.js';
export const USE_STATE_HOOK_ID = 'Component.useState-0';
export const USE_ASYNC_HOOK_ID = 'Component.useAsync-1';
export const meta = {
    [Header.AppUser]: {
        values: ['t2_appuser'],
    },
    [Header.Subreddit]: {
        values: ['t5_devvit'],
    },
    [Header.App]: {
        values: ['app'],
    },
    [Header.Installation]: {
        values: ['install'],
    },
};
export const UI_STATE = {
    Loading: 'I am Loading...',
    DataLoaded: 'Data loaded!',
    Error: 'Encountered Error...',
};
export const emptyRequest = () => ({
    events: [],
});
export function serializeLoadedDataState(data) {
    return `${UI_STATE.DataLoaded} :  ${JSON.stringify(data)}`;
}
